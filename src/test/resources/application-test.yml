spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

canvas:
  api:
    base-url: http://localhost:8080/mock-canvas-api
    timeout: 5000

logging:
  level:
    com.studytracker: DEBUG